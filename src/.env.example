# Public app always on :3000. Browser must use this (Next proxies /api → :5001).
NEXT_PUBLIC_API_URL=http://localhost:3000
# Internal Express only (macOS AirPlay often blocks :5000).
API_URL=http://localhost:5001
