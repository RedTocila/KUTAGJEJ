# API Configuration (no trailing slash). Browser calls this host + /api/...
# Local: Express default port. Production: set the same in Vercel → Project → Environment Variables
# (must be YOUR backend URL, e.g. https://your-api.vercel.app — not a different app’s API).
NEXT_PUBLIC_API_URL=http://localhost:5000
