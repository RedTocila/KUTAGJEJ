import * as React from 'react';

import { PostListingFormSkeleton } from '@/components/user/post-listing-header';

export default function Loading(): React.JSX.Element {
  return <PostListingFormSkeleton />;
}
